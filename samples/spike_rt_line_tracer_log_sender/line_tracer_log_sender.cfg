// SPDX-License-Identifier: MIT
/*
 * line_tracer_log_sender.cfg
 *
 * TOPPERS/ASP カーネルのシステムコンフィギュレーションファイル。
 * 本アプリは単一タスク構成で、起動と同時に main_task を実行します。
 */
INCLUDE("tecsgen.cfg");

#include "line_tracer_log_sender.h"

/* TA_ACT: カーネル起動時に即座に main_task を実行開始 */
CRE_TSK(MAIN_TASK, { TA_ACT, 0, main_task, MAIN_PRIORITY, STACK_SIZE, NULL });
